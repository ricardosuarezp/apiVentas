
services:
  postgres-db:
    image: postgres:16-alpine
    container_name: apirest_ventas_db
    restart: always
    environment:
      POSTGRES_DB: db_ventas_api
      POSTGRES_USER: adminpostgress
      POSTGRES_PASSWORD: adminsapiventas2026
    ports:
      - "5445:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: